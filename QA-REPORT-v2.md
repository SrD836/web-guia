# QA Report v2 — web-guia prototype (post-redesign regression)

**Date:** 2026-06-07  
**Reviewer:** Dwight (wg-8)  
**Verdict:** **PASS**  
**Browser check:** RAN — Playwright 1.60.0, Chromium, `file://` protocol  
**Scope:** Regression QA after Pam's full visual redesign (5 HTML + styles.css + app.js rewrite)  
**v1 QA fixes confirmed applied:** QA fix #1 (openclaw-eco pill removed), QA fix #2 (concepto.html ToC anchors)

---

## Check 1 — Nav: 5 links, aria-current, hamburger SVG ✅ PASS

Playwright result (Chromium, desktop viewport):

| Page | Desktop links | Mobile links | aria-current | Hamburger SVG |
|------|--------------|--------------|--------------|---------------|
| index.html | 5 | 5 | "Inicio" | ✓ |
| agentes-cli.html | 5 | 5 | "Agentes CLI" | ✓ |
| orquestadores.html | 5 | 5 | "Orquestadores" | ✓ |
| concepto.html | 5 | 5 | "Conceptos" | ✓ |
| acerca.html | 5 | 5 | "Acerca" | ✓ |

Hamburger button initial HTML is inline SVG (`<svg width="20" height="20"...>`) on all 5 pages — confirmed `hamburgerIsSVG: true`. Zero JS errors on all pages.

**Hamburger toggle note (methodology):** Playwright's default viewport is desktop (1280px+), where `md:hidden` CSS hides the hamburger button, so the `page.click('#navToggle')` call was a no-op. The toggle logic was verified via static analysis: `initNavToggle()` in app.js correctly sets `toggle.innerHTML = NAV_OPEN_SVG | NAV_CLOSE_SVG` and toggles `hidden`/`flex` on `#navMobile`. Recommend manual check at 360px viewport before launch.

---

## Check 2 — Card counts ✅ PASS

Playwright result:

| Page | Expected | Actual |
|------|----------|--------|
| agentes-cli.html total | 16 | 16 ✅ |
| orquestadores.html total | 14 | 14 ✅ |
| index.html featured | 6 | 6 ✅ |

Index featured logic: collects all items with `badge === 'Popular'` from `cli[]` (5 items) + `orquestadores[]` (2 items) = 7, sliced to 6 ✓

---

## Check 3 — Filters: QA fix #1 confirmed + all filters work ✅ PASS

### agentes-cli.html — QA fix #1 confirmed

Playwright result:

| Check | Expected | Actual |
|-------|----------|--------|
| Pills present | 3 (todos/open-source/closed) | 3 ✅ |
| `openclaw-eco` pill | ABSENT | absent ✅ |
| `open-source` filter | 13 visible | 13 ✅ |
| `todos` reset | 16 visible | 16 ✅ |
| openclaw-eco cards in grid | 0 | 0 ✅ |

**QA fix #1 CONFIRMED:** The "Ecosistema OpenClaw" pill has been removed. HTML source includes the comment `<!-- QA fix: removed openclaw-eco pill (no entries in dataset) -->`. Only 3 pills present: `["todos","open-source","closed"]`.

### orquestadores.html

| Check | Expected | Actual |
|-------|----------|--------|
| Pills present | 5 | 5 ✅ |
| `swarms` filter | 4 visible | 4 ✅ |
| `todos` reset | 14 visible | 14 ✅ |

Pills: `["todos","runners-paralelos","swarms","loops-autonomos","asistentes"]` ✓

---

## Check 4 — QA fix #2: concepto.html ToC anchors ✅ PASS

Playwright result:

| Check | Expected | Actual |
|-------|----------|--------|
| Section IDs | sec-1 … sec-7 | `["sec-1","sec-2","sec-3","sec-4","sec-5","sec-6","sec-7"]` ✅ |
| ToC hrefs | #sec-1 … #sec-7 | `["#sec-1","#sec-2","#sec-3","#sec-4","#sec-5","#sec-6","#sec-7"]` ✅ |
| IDs match hrefs | all | `allIdsMatch: true` ✅ |
| IntersectionObserver | active | inline script present, correct rootMargin `-15% 0px -70% 0px` ✅ |

**QA fix #2 CONFIRMED:** All 7 article sections have `id="sec-N"`. All 7 ToC links point to `href="#sec-N"` with matching `data-toc-id="sec-N"`. Clicking a ToC link now scrolls to the correct section instead of page top.

---

## Check 5 — External links target=_blank rel=noopener ✅ PASS (with pre-existing note)

- Card links: generated by `renderCards()` in app.js — `target="_blank" rel="noopener"` on all card "Ver →" links ✓
- acerca.html source credit cards: both GitHub links have `target="_blank" rel="noopener"` ✓

**Pre-existing LOW (not a regression):** Footer nav links on all 5 pages (`Orquestadores (fuente)`, `CLIs (fuente)`) link externally to GitHub but lack `target="_blank" rel="noopener"`. This was present in v1 and not flagged there; it is not introduced by the redesign.

---

## Check 6 — Console errors ✅ PASS

Zero real JS errors across all 5 pages (Playwright `pageerror` events, Tailwind CDN warning excluded per spec).

```
index.html        → jsErrors: []
agentes-cli.html  → jsErrors: []
orquestadores.html→ jsErrors: []
concepto.html     → jsErrors: []
acerca.html       → jsErrors: []
```

Tailwind CDN "should not be used in production" warning present as expected — not flagged.

---

## Check 7 — Spanish prose, no caveman in UI ✅ PASS

All visible text is natural full-prose Spanish. Spot-checked:
- Hero: "Automatiza tu flujo de código con agentes de IA" ✓
- Hero body: "Descubre cómo los agentes de código CLI y orquestadores transforman la forma…" ✓
- agentes-cli.html intro: "Un agente de código CLI es un programa que corre en tu terminal y usa inteligencia artificial…" ✓
- Card descriptions: all full sentences, no fragments ✓
- Filter pills: "Todos", "Código abierto", "Cerrado / comercial", "Ejecutores en paralelo", "Enjambres", "Bucles autónomos", "Asistentes" ✓
- No caveman language detected anywhere in user-facing text ✓

---

## Check 8 — Responsive 360/768/1280 ✅ PASS (static analysis)

- Card grids: `grid-cols-1 md:grid-cols-2 xl:grid-cols-3` on agentes-cli.html, orquestadores.html, index.html featured ✓
- Learning-path grid (index): `grid-cols-1 md:grid-cols-2` ✓
- How-to-use grid (index): `grid-cols-1 md:grid-cols-3` ✓
- Nav: desktop `hidden md:flex`, hamburger `md:hidden` ✓
- Max-width `max-w-content` (1152px) + `px-6` gutters on all sections ✓
- Hero type: `text-4xl md:text-5xl xl:text-[3.25rem]` ✓

*Note: Recommend manual check at 360px (hamburger + single-column grid) before public launch.*

---

## v1 findings resolution

| v1 Finding | Status |
|------------|--------|
| LOW — openclaw-eco pill always "Sin resultados" | ✅ FIXED — pill removed |
| LOW — concepto.html ToC href="#" (no ids on sections) | ✅ FIXED — sec-1…sec-7 ids + matching hrefs |
| LOW — Decorative emoji not aria-hidden (index.html) | ✅ FIXED — emoji replaced with inline SVG icons with aria-hidden="true" |
| INFO — renderCards() innerHTML without HTML escaping | ⏳ UNCHANGED — still present, still no injection risk with current data |

---

## Summary of new/remaining findings

| # | Severity | Area | Finding | Blocker? |
|---|----------|------|---------|----------|
| 1 | LOW | a11y/UX | Hamburger toggle not browser-tested (desktop viewport hides button via `md:hidden`; static analysis confirms JS logic correct) | No |
| 2 | LOW | Security | Footer external GitHub links missing `target="_blank" rel="noopener"` — pre-existing from v1, not a regression | No |
| 3 | INFO | Code | `renderCards()` innerHTML concat without HTML escaping — carried from v1, no risk with current data | No |

No blockers. Zero JS errors. All card counts exact. All filters correct. Both QA fixes confirmed applied. Three v1 LOWs resolved.

**Overall verdict: PASS** — redesigned prototype is shippable. Remaining items (1–3) are cosmetic or pre-existing; none affect core functionality.
